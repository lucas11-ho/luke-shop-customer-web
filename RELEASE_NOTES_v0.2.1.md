# Customer Web v0.2.1 — Dynamic Tenant Routing

Replaces the fixed build-time demo tenant behavior with route/hostname-based storefront resolution. Customer sessions are tenant-bound and are cleared when the resolved tenant changes. Public routes consume published experience only; signed preview paths are separately resolved by Backend.
