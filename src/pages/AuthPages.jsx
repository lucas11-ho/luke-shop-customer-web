import React,{useState}from'react';import{useAuth}from'../auth/AuthContext.jsx';import{go}from'../app/router.js';
function AuthCard({mode,next}){const{login,register}=useAuth();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[name,setName]=useState('');const[busy,setBusy]=useState(false);const[error,setError]=useState('');const isRegister=mode==='register';
 const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{if(isRegister)await register({email,password,displayName:name});else await login({email,password});go(next||'/');}catch(err){setError(err.message);}finally{setBusy(false);}};
 return <section className="auth-page"><div className="auth-card"><span className="eyebrow">Customer account</span><h1>{isRegister?'Create your account':'Welcome back'}</h1><p>{isRegister?'Register to manage your cart, checkout and track orders.':'Sign in to continue shopping and track your orders.'}</p><form onSubmit={submit}>{isRegister&&<label>Display name<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Your name"/></label>}<label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></label><label>Password<input required type="password" minLength="12" maxLength="128" value={password} onChange={e=>setPassword(e.target.value)} autoComplete={isRegister?'new-password':'current-password'}/><small>Minimum 12 characters.</small></label>{error&&<div className="form-error">{error}</div>}<button className="btn btn-primary btn-full" disabled={busy}>{busy?'Please wait…':isRegister?'Create account':'Sign in'}</button></form><div className="auth-switch">{isRegister?'Already have an account?':'New customer?'} <button onClick={()=>go(isRegister?'/login':'/register',{next})}>{isRegister?'Sign in':'Create account'}</button></div></div></section>;
}
export function LoginPage({next}){return <AuthCard mode="login" next={next}/>;}export function RegisterPage({next}){return <AuthCard mode="register" next={next}/>;}
