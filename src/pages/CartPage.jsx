import React,{useState}from'react';import{useCart}from'../cart/CartContext.jsx';import{useAuth}from'../auth/AuthContext.jsx';import{useStore}from'../store/StoreContext.jsx';import{Spinner,Empty,money,Toast}from'../components/UI.jsx';import{go}from'../app/router.js';
export function CartPage(){const{isAuthenticated}=useAuth();const{tenant}=useStore();const{cart,loading,updateItem,removeItem}=useCart();const[busy,setBusy]=useState('');const[toast,setToast]=useState('');if(!isAuthenticated)return <section className="section"><Empty title="Sign in to view your cart" body="Your cart is connected to your customer account." action={<button className="btn btn-primary" onClick={()=>go('/login',{next:'/cart'})}>Sign in</button>}/></section>;if(loading&&!cart)return <Spinner/>;const items=cart?.items||[];const mutate=async(fn,id)=>{setBusy(id);try{await fn();}catch(e){setToast(e.message);}finally{setBusy('');}};return <section className="section"><div className="section-head"><div><span className="eyebrow">Your bag</span><h1>Cart</h1></div></div>{!items.length?<Empty title="Your cart is empty" body="Browse the storefront and add something you like." action={<button className="btn btn-primary" onClick={()=>go('/explore')}>Explore products</button>}/>:<div className="cart-layout"><div className="cart-list">{items.map(item=><article className="cart-item" key={item.public_id}>{item.media_url?<img src={item.media_url} alt=""/>:<div className="cart-thumb media-placeholder">Item</div>}<div className="cart-item-info"><button className="item-title" onClick={()=>go(`/product/${item.product_slug}`)}>{item.title_snapshot}</button>{item.variant_title_snapshot&&<span>{item.variant_title_snapshot}</span>}<span>{item.fulfillment_mode.replaceAll('_',' ')}</span>{item.selected_modifiers?.length>0&&<small>{item.selected_modifiers.map(m=>m.name).join(', ')}</small>}<strong>{money(item.line_total,item.currency,tenant?.locale)}</strong></div><div className="cart-controls"><div className="qty"><button disabled={busy===item.public_id||item.quantity<=1} onClick={()=>mutate(()=>updateItem(item.public_id,item.quantity-1),item.public_id)}>−</button><span>{item.quantity}</span><button disabled={busy===item.public_id} onClick={()=>mutate(()=>updateItem(item.public_id,item.quantity+1),item.public_id)}>+</button></div><button className="text-danger" disabled={busy===item.public_id} onClick={()=>mutate(()=>removeItem(item.public_id),item.public_id)}>Remove</button></div></article>)}</div><aside className="summary-card"><h3>Order summary</h3><div><span>Subtotal</span><strong>{money(cart.totals?.subtotal,cart.currency,tenant?.locale)}</strong></div><div><span>Delivery</span><span>Calculated at checkout</span></div><hr/><div className="summary-total"><span>Estimated total</span><strong>{money(cart.totals?.grand_total,cart.currency,tenant?.locale)}</strong></div><button className="btn btn-primary btn-full" onClick={()=>go('/checkout')}>Continue to checkout</button></aside></div>}<Toast message={toast} type="bad" onClose={()=>setToast('')}/></section>;}
