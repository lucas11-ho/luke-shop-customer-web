# Luke Shop Customer Web v0.9.0 — Commerce Connector v2

Adds a secure embedded Luke CS chat modal. Signed Shop customer context is delivered only by exact-origin `postMessage`, never in the iframe URL. Order Detail requests a customer-owned order hint; Chat can request a fresh short-lived context before expiry.
