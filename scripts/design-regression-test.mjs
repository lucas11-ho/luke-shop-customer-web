import fs from 'node:fs';const files=['src/styles.css','src/components/Shell.jsx','src/pages/HomePage.jsx','src/components/ProductCard.jsx'];let all='';for(const f of files){if(fs.existsSync(f))all+=fs.readFileSync(f,'utf8').replace(/\r\n?/g,'\n')+'\n'}let passed=0;const check=(v,m)=>{if(!all.includes(v))throw new Error(`Missing ${m}`);console.log(`PASS ${m}`);passed++};check('professional-storefront','professional storefront shell');check('search-action','storefront search action');check('campaign-media','tenant campaign media');check('category-kicker','adaptive category merchandising');check('discount-chip','discount merchandising');check('stock-note','stock state merchandising');check('active-promo-rail','scheduled promotion merchandising rail');check('--font-heading','typography renderer token');check('data-product-grid','template product layout renderer');console.log(`${passed}/${passed} Customer Web v0.5.0 design checks passed`);
